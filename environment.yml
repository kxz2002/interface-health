name: interface
channels:
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      # torch 不在此处安装，请在 conda env create 后根据设备手动运行：
      #   CPU:  make torch-cpu
      #   GPU:  make torch-gpu  (按需修改 Makefile 中的 CUDA 版本)
      - numpy==2.1.2
      - pandas==2.2.3
      - hydra-core==1.3.3
      - omegaconf==2.3.1
      - scikit-learn==1.5.2
      - scipy==1.14.1
      - pyyaml==6.0.3
      - matplotlib==3.9.2
      - drain3-improved==0.10.0
      - dvc==3.67.1
      - mlflow==3.13.0
      - pyarrow==17.0.0
      # dev
      - pytest==9.0.3
      - pre-commit==4.6.0
