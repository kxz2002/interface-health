# CI-only dependency pin. Installed by .github/workflows/ci.yml after torch but
# before `pip install -e .[dev]`. Pinned to the versions that were working on
# master as of 2026-08-24 (last green CI). Without this pin, the bare
# `pyproject.toml` floor `pandas>=2.0, numpy>=1.24` allows the resolver to
# pick up pandas 3.0.5 / numpy 2.5.3 (released between Aug 24 and Sep 8 2026),
# which exposes a pre-existing read-only-array bug in
# src/data/endpoint_baseline_stats.py:82 (and src/fusion/deviation_weighted.py).
# That bug is not in this PR's scope and is being tracked as a follow-up PR.
#
# DO NOT widen this pin without verifying CI against pandas 3 / numpy 3.
numpy==2.1.2
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.5.2
pyarrow==17.0.0
drain3-improved==0.10.0
hydra-core==1.3.3
omegaconf==2.3.1
pydantic==2.13.4
