defaults:
  - data: anomod
  - model: deep_svdd
  - fusion: concat
  - training: default
  - _self_

# 可复现性
seed: 42

# 输出目录，按时间戳组织
output_dir: outputs/${now:%Y-%m-%d_%H-%M-%S}

# Contract 数据目录与训练产物输出路径，替代原 argparse 的 --contract-dir/--out
contract_dir: ???
out: ???
